;; Kanata configuration for keyboard key remapping


;; Remapping CapsLock key to Esc/LCtrl with Mod-Tap Behavior
(defsrc
    caps
)

(defalias
    escctrl (tap-hold 100 100 esc lctrl)
)

(deflayer base
    @escctrl
)
